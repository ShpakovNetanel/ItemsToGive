export enum UserTypes {
  DONOR = "DONOR",
  DONATED = "DONATED",
  SHIPPER = "SHIPPER",
}

export enum ItemStatus {
  TO_DONATE = 0,
  TO_SHIP = 1,
}

export enum DisplayedItems {
  MY_ITEMS = 0,
  OTHERS_ITEMS = 1,
  TO_SHIP_ITEMS = 2,
}

export enum HeaderTabs {
  NEAR_ME = "NEAR_ME",
  ALL_ITEMS = "ALL_ITEMS",
}
