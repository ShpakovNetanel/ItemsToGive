export enum UserTypes {
  DONOR = "DONOR",
  DONATED = "DONATED",
  SHIPPER = "SHIPPER",
}

export enum ItemStatus {
  TO_DONATE = 0,
  TO_SHIP = 1,
  MY_ITEM = 2,
}

export enum ItemCategoery {
  MY_ITEMS = 0,
  HOME = 1,
  TO_SHIP_ITEMS = 2,
  NEW_POST = 3,
}

export enum ItemClause {
  NEAR_ME = "NEAR_ME",
  ALL_ITEMS = "ALL_ITEMS",
  MY_ITEMS = "MY_ITEMS",
}
